{% if app.user and app.user.isDemo %}
    <div class="bg-yellow-500/15 border-b border-yellow-500/40 px-4 py-2 flex items-center justify-center gap-3 text-xs text-yellow-300">
        <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <span>{{ 'demo.banner'|trans({'%fileSizeMb%': demoMaxFileSizeMb, '%totalSizeMb%': demoMaxTotalSizeMb}) }}</span>
    </div>
{% endif %}
